# KIND cluster configuration — NOT USED.
#
# This experiment uses a managed Kubernetes cluster (3 nodes, 4 vCPU, 8 GB RAM each).
# KIND config is not applicable but this directory is kept for structural consistency
# with the implementation.md specification.
#
# If you need to run this experiment with KIND instead, create a kind-config.yaml here:
#
# kind: Cluster
# apiVersion: kind.x-k8s.io/v1alpha4
# nodes:
# - role: control-plane
# - role: worker
# - role: worker
# - role: worker
